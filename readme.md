Before launching the application in flask, run app.py in ipython, and then call the setup() function. This will create all the tables in the database and generate the tags as well.

http://colormind.io/
